bcr_test_module:
  module_path: "."
  matrix:
    platform: ["debian10", "ubuntu2004", "macos"]
    bazel: [7.x, 8.x]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
