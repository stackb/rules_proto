# if you update the Gemfile, re-run bundle in this directory to regenerate the
# Gemfile.lock.  Unfortunately, this seems to cause duplicate rules to be
# generated by the ruby_bundle, so de-duplicate the Gemfile.lock (arch-specific
# versions) first?
update_gemfile_lock:
	bundle install