syntax = "proto2";

package proto;

/**
 * Used to signify the end of the snapshot portion of a stream
 */
message Complete {
  /* The number of snapshot messages sent */
  required uint32 expected_messages = 1;
}
