---
buildifier:
  version: 4.0.1
  warnings: "-function-docstring,-function-docstring-header"
bazel: 4.0.0
tasks:
  default_workspace_ubuntu1804:
    platform: ubuntu1804
    test_targets:
      - //pkg/...
      - //example/...
    test_flags:
      - --config=bazelci
  default_workspace_macos:
    platform: macos
    test_targets:
      - //pkg/...
      - //example/...
