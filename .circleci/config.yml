version: 2
jobs:
  build19:
    docker:
      - image: gcr.io/stack-build/rules_proto/bazel:0.19.2
    steps:
      - checkout
      - run: make
  build20:
    docker:
      - image: gcr.io/stack-build/rules_proto/bazel:0.20.0
    steps:
      - checkout
      - run: make
workflows:
  version: 2
  build_and_test:
    jobs:
      - build19
      - build20
